# Tor v3 hidden service private key material — must never be committed.
# A leaked hs_ed25519_secret_key permanently deanonymizes the .onion address.
# Track the directory itself (via .gitkeep) but ignore all key files inside it.
keys/*
!keys/.gitkeep
